# AWS Resource Tracker

This is a script to track AWS resources (EC2, S3, Lambda, IAM users, etc.) in your project.

## Requirements:
- AWS CLI installed
- jq for parsing JSON

## Usage:
Run the script to fetch resource information from AWS:
```bash
./aws_resource_tracker.sh
	
